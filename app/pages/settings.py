"""Settings — stub until SOS-31."""

from __future__ import annotations

import streamlit as st


def render_settings() -> None:
    st.title("Settings")
    st.info("Connector, weights and AI settings ship in SOS-31.")
